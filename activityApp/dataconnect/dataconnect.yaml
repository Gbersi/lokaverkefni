specVersion: "v1beta"
serviceId: "activityApp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "activityApp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
